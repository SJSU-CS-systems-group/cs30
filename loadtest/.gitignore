# Generated by gen-fixtures.py — roster, sessions and IP list are environment-specific
students.json
loadtest-course.yaml
seed-sessions.sql
local-ips.txt
# Environment config and live logs
loadtest-application.properties
loadtest-backend.log
# k6's raw metric stream: bulky, machine-only, nothing reads it routinely.
# The summary/meta/metrics/submissions artifacts are the evidence that gets kept.
results/*-raw.json

# Environment-specific: names the problems a lab uses. Use loadtest-lab1.yaml.example as a template.
loadtest-lab1.yaml
